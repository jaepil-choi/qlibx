# Attempt 1

Status: archived reference
Source branch: `exp/one-shot`
Source commit before archival: `ee41c5414f278d5e47f5b4657edbc7b8c52ea06a`

This directory preserves the first qlibx implementation so its code, tests, configuration,
examples, and package metadata remain available while the repository root is rebuilt from the new
canonical PRD.

The archive includes:

- `src/`, `config/`, `tests/`, `experiments/`, `showcases/`, and `qlibx-custom/`;
- the original `pyproject.toml`, `uv.lock`, `README.md`, and `qlibx.yaml`;

Documentation, shared source data, external reference snapshots, repository workflow
infrastructure, virtual environments, caches, and runtime state remain outside the archive.

Attempt 1 is frozen. Do not implement new behavior here; use it only as evidence and reference for
the second attempt.
