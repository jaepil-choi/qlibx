# NautilusTrader upstream snapshot

- Source: https://github.com/nautechsystems/nautilus_trader
- Branch at import: `develop`
- Commit: `4d14b8c669f8fc31f343da9ae39526729a78381f`
- Imported on: 2026-07-31
- Local path: `references/nautilus_trader/`

All 5,966 paths tracked by the upstream repository at the commit above were
imported. Git metadata is intentionally excluded so this directory is ordinary,
non-authoritative reference content in the qlibx repository. The upstream
`LICENSE` is included at `references/nautilus_trader/LICENSE`.

Four local metadata files are added outside the upstream snapshot:
`UPSTREAM.md` and three nested `.gitignore` files. The nested ignore files only
make 52 upstream-tracked data and script paths visible to the parent repository;
the upstream root `.gitignore` otherwise hides those tracked paths after its
original Git index is removed.

The upstream tree contains 42 symbolic-link entries. Git for Windows checked
these out as regular files containing their link targets, so their contents are
present but their symbolic-link filesystem mode is not preserved in this
vendored snapshot.
