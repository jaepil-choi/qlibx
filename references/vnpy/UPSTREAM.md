# vn.py upstream snapshot

- Source: https://github.com/vnpy/vnpy
- Branch at import: `master`
- Commit: `1b78494979deb4c4996f6b864f234d9839f2f239`
- Imported on: 2026-07-31
- Local path: `references/vnpy/`

All 196 files tracked by the upstream repository at the commit above were
imported without modification. Git metadata is intentionally excluded so this
directory is ordinary, non-authoritative reference content in the qlibx
repository. The upstream `LICENSE` is included at `references/vnpy/LICENSE`.

Two local metadata files are added outside the upstream snapshot:
`UPSTREAM.md`, and `docs/.gitignore`. The latter only makes seven upstream-
tracked documentation assets visible to the parent repository because the
upstream `.gitignore` ignores `_static` and `_templates` directories.
