"""vqapr — point-in-time quantitative research and portfolio execution framework.

이 package의 유일한 documented surface는 `vqapr.public`이다. 내부 module 경로는 계약이 아니며
리팩터로 바뀔 수 있다(architecture §2.6, `UC-FACADE-001`).

여기에는 re-export를 두지 않는다. facade가 `public.py` 하나여야 "package source를 열지 않고
완주"가 성립한다.
"""
