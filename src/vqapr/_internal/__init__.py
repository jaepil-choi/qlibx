"""Private implementation package for `vqapr`.

Nothing under `vqapr._internal` is part of the supported public surface. A caller outside the
`vqapr` package must never import from here; every supported capability has a home in one of the
top-level `vqapr.*` modules instead.
"""

from __future__ import annotations
