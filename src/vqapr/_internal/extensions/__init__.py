"""Private extension identity, loading, and registration engine.

This package holds the retained authority machinery that fingerprints, loads, and registers
project-local and built-in `vqapr` extension components (data models, strategy models, and
constraints). It is not part of the public API.
"""

from __future__ import annotations
