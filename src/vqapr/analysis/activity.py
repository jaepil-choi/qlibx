"""저장된 fill -> 거래 활동.

구현할 것
    **실현 회전율** · 체결 건수 · 비용 분해(fee/tax) · 매수/매도 규모

`portfolio/diagnostics.py`의 의도 회전율과 다르다
    이쪽은 체결 기록에서, 저쪽은 판단 시점 weight 차이에서 나온다. 정수 변환과 미체결 때문에
    **두 값은 당연히 다르다.** 섞어 읽으면 PRD §2.2가 경고하는 혼동이 생긴다.

회전율은 선언이 아니라 결과다
    "평균 리밸런싱 주기 63거래일"은 여기서 계산한 회전율에서 역산한 통계이지 cadence 선언이
    아니다(architecture §3.4).

보유 대비로 측정한다
    어제의 목표와 오늘의 목표를 빼면 벤치마크 표류까지 거래로 센다. 실제 보유에서 실제 체결로
    측정해야 "아무 일도 안 했는데 생긴 회전율"이 사라진다(architecture §11.7 ⑤).
"""
