"""cli — **파일 목록 = 명령어 목록.**

명령이 늘면 파일이 늘고, 파일이 안 늘면 명령도 안 는다. CLI가 곧 제품 표면이라 그 대응이
깨지면 보이지 않는 명령이 생긴다.

    new       확장점 템플릿 설치
    check     conformance 검사 (기계 판독)
    register  fingerprint를 찍어 등록
    data      dataset 등록과 조회
    run       run 실행 / materialize
    report    저장된 결과를 읽어 표로
    agent     onboarding preview / apply / update / remove
"""
