"""`vqapr report <run-id>` — 저장된 결과를 읽는다.

구현할 것
    `analysis/renderers.py`를 부른다
    table 또는 machine-readable 출력 선택

**producer를 재실행하지 않는다.**
    저장된 것만 읽는다(`UC-REPORT-001`).

**그림을 만들지 않는다.**
    chart는 user가 같은 값 위에 구성한다. 이 명령은 값과 표를 낸다.
"""
