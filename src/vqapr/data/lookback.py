"""얼마나 과거까지 읽는가 — 그리고 미래 방향 타입은 없다.

구현할 것
    RowsLookback       PIT gate를 통과한 행을 logical key로 정렬한 뒤
                       **(instrument x field)별로** 최근 N행
    CalendarLookback   evaluation date에서 years/months/days를 달력 산술로 이동한 date의
                       00:00부터 evaluation time까지. 거래일 수를 세는 것이 **아니다**
    Lookback           위 둘의 union

**이 파일에 미래 방향 타입이 없다는 것 자체가 계약이다.**
    타입이 존재하지 않으므로 어떤 소비자도 미래 관측을 당겨 읽을 수 없다. label처럼 미래가
    필요해 보이는 계산은 값을 나중 시점에 기록하고 소비자가 시점을 맞춰 읽는다 —
    "t의 20일 후 수익률"은 "t+20에 기록된 20일 수익률"과 같은 값이다(PRD §3.5).

    별도 파일로 둔 이유가 이것이다. 이 부재를 파일 하나를 열어 확인할 수 있어야 한다.

왜 rows가 (instrument x field)별인가
    분기 재무는 항목마다 공시 시점이 다를 수 있어 "각 항목의 최근 20개"가 "최근 20개 시점"보다
    정확하다. 넓은 표에서 모든 field가 같은 행에 있으면 두 해석의 결과가 같으므로 배치와
    무관하게 같은 규칙을 쓴다.

    이것이 없으면 field 폴더에서 RowsLookback(20)이 field 5개일 때 field당 4행이 되고,
    "있는 만큼 반환한다"에 걸려 **실패하지 않고 조용히 절반만 온다**(architecture §4.2).

vocabulary를 늘리지 않는다
    "이번 달 행만" 같은 경계를 여기 추가하지 않는다. 넉넉히 받아 소비자가 거른다. 경계를
    타입으로 만들면 "이번 달"이 월초인지 첫 거래일인지가 또 결정 대상이 된다.

`UC-LOOKBACK-001`
"""
