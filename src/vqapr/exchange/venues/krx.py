"""KRX venue.

**이름에 cadence가 없다.**
    venue가 소유하는 것 중 daily와 minutely 사이에서 달라지는 것이 하나도 없다 — listing
    수량 단위도, cost 요율도, 아래 체결 알고리즘도 같다. cadence는 전부 `conventions.py`의
    FillConvention과 체결 테이블의 행 밀도라는 **두 선언**에 있다. 이름에 구우면 나중에
    krx_minutely가 생겨 전부 복제한다.

구현할 것
    Exchange 계약의 구현
        direction  long-only
        quantity   listing의 정수 step
        cost       effective-dated fee/tax + cash clipping
        fill       지원 order 전량
        realism    "simulation"

체결 알고리즘 — 매도 전량 -> 매수
    1. 각 주문의 수량을 먼저 정한다       목표금액 / 체결가 -> 정수 내림
    2. 그 수량의 실제 소요액을 구한다      수량 x 가격 + 비용
    3. delta 내림차순으로 누적한다         동률은 instrument_id 사전순
    4. 현금을 넘는 지점 — 그 종목은 가능한 만큼, 이후는 0주

    왜 1이 3보다 먼저인가   목표 금액으로 누적하면 **있는 현금을 못 쓴다.** 내림 때문에 각
                            주문이 조금씩 적게 나가고 100종목이면 그 잔여가 쌓인다
    왜 delta 기준인가       실패했을 때 잃는 것은 delta로 잰다. 이미 9.9% 들고 있는 종목을
                            먼저 채워도 얻는 것이 없다
    왜 tie-break가 필요한가 균등가중은 전 종목이 동률이다. 정하지 않으면 컨테이너 순서가
                            결과를 바꿔 deterministic replay가 깨진다
    왜 매도가 먼저인가      대금이 매수를 조달하고, **인과가 진단에 남는다** —
                            "A 매도 실패(정지) -> 현금 부족 -> C·D 매수 실패"

비례 축소를 쓰지 않는다
    "모든 종목을 똑같이 깎는다"도 경제적 선택이지 중립이 아니다. 비례는 **전부를 틀리게** 하고
    delta 우선은 **대부분을 정확히** 만들고 일부만 포기한다. 진단도 비교가 안 된다.

순차 의미론, 벡터 구현
    위 규칙은 순서로 정의되지만 순차로 구현할 필요가 없다. 정렬 -> 정수 내림 -> 소요액 ->
    누적합 -> 초과 지점 -> 경계 하나만 조정. **적어두지 않으면 for 루프를 돈다**(`UC-SCALE-001`).

두 경로, 같은 결과
    빠른 경로   sum(목표매수) + sum(예상비용) <= 현금 + sum(예상매도대금) 이면 각 주문 독립 계산
    느린 경로   그 외는 위 정렬 + 누적합

    내림은 항상 소요액을 줄이므로 빠른 경로 조건에서는 아무도 실패하지 않는다. **성능 경로는
    결과 동일성이 증명될 때만 둔다.** 사용자가 고르는 모드가 아니라 구현 내부의 최적화다.

미모델링
    partial fill · volume impact · 실제 결제
"""
