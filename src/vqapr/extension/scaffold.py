"""템플릿을 임의 디렉터리에 설치한다.

구현할 것
    scaffold(kind, target_dir) — `templates/<kind>/`의 파일을 target에 복사
    preview                     무엇이 생기고 무엇이 덮이는지 먼저 보여준다
    기존 파일 보호               확인 없이 덮어쓰지 않는다

왜 임의 디렉터리인가
    사용자는 `./my_strategy`처럼 자기가 고른 곳에서 시작하고 싶어 한다. 패키지가 위치를
    강제하면 project 구조를 패키지가 소유하게 된다.

설치되는 것
    구현 파일 + yaml 선언 + **자기 conformance 테스트** + README. 템플릿이 자기 테스트를 들고
    나오므로 통과 조건이 문서가 아니라 실행 가능한 형태로 전달된다.

    그 테스트는 **처음에 실패한다.** 그것이 정상이고, 실패가 계약을 읽게 만든다.
"""
