"""판단 시점의 배분 진단.

구현할 것
    gross / net exposure · 집중도 · 유효 종목 수
    **의도 회전율**  sum_i |w_i - w0_i|

`analysis/activity.py`의 실현 회전율과 의도적으로 분리한다
    입력이 다르다 — 이쪽은 판단 시점의 weight 차이고 저쪽은 체결 기록이다. 정수 변환과
    미체결 때문에 두 값은 **당연히 다르다.** PRD §2.2가 경고하는 혼동이 정확히 여기라
    이름과 파일이 갈려 있어야 한다.

    그리고 의도 회전율은 성과 보고의 출처가 될 수 없다. turnover를 주장하려면 execution
    result에서 나와야 한다(`UC-RETURN-001`).

순수 함수다
    같은 leaf 규칙을 받는다. 계좌도 데이터도 읽지 않고 weight 벡터만 받는다.
"""
