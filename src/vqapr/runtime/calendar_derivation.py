"""calendar를 유도해야 할 때 — 규칙은 닫힌 집합이다.

상황
    사용자가 가진 것이 daily OHLCV뿐이고 거래소 calendar 파일이 없다. 가장 흔한 출발점이다.

구현할 것
    유도 규칙의 **닫힌 집합**과 각각의 검증
        전 종목 날짜 union    하루라도 거래된 날이 session. 한 종목의 결측에 무너지지 않는다
        단일 기준 종목의 날짜  그 종목이 정지되면 session이 사라진다. 위험
        지수 시계열의 날짜     안전. 다만 지수 데이터가 있어야 한다
    선택된 규칙과 그 한계를 frozen input에 기록하는 경로

왜 별도 파일인가
    유도 규칙이 **닫힌 집합**이라는 사실이 파일 하나로 보여야 한다. 흩어지면 "package는
    추측하지 않는다"가 부재로 표현되지 않는다.

날짜는 유도되고 시각은 유도되지 않는다
    daily 행에는 `2024-03-05`만 있고 `15:30 KST`가 없다. 그 시각은 이미 `available_at` 규칙
    (`data/availability.py`)이 담고 있으므로 새로 요구하지 않고 같은 선언을 재사용한다.

`data/availability.py`와 같은 모양이다
    package는 추측하지 않고, user가 근거와 함께 규칙을 고르고, package가 deterministic하게
    검증하고, 규칙이 frozen input에 남는다. 두 파일이 대칭이어야 그 사실이 코드에 보인다.

`UC-CALENDAR-001`
"""
