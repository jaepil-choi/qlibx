"""DataModel 계약 검사.

검사할 것
    `compute()`가 Rows를 반환한다
    선언한 requirement 밖의 데이터를 읽지 않는다
    **창 밖을 읽지 않는다**
    `available_at`을 스스로 주장하지 않는다
    Model state를 쓰면 순차 생성 표시가 출력에 남는다
    같은 입력에 같은 출력 (state 없는 경우)
    trigger 선언이 해석 가능하다
    tables() 선언이 예약 컬럼을 쓰지 않는다

왜 "창 밖을 읽지 않는다"를 검사할 수 있나
    창이 사각형 하나이고 그 밖에 접근할 경로가 없으므로, 선언보다 넓은 데이터가 결과에 반영
    되었는지를 알려진 픽스처로 판정할 수 있다.
"""
