"""노출을 회귀로 제거한다.

구현할 것
    선언된 노출(업종, 스타일, 크기 등)에 대해 신호를 중립화하는 순수 함수.
    노출 행렬은 **인자로 받는다.** 업종 분류를 여기서 읽지 않는다.

왜 이 파일이 특별한가
    `UC-EXTENSION-001`이 지목한 원본이다 — "built-in 예시를 참고해 agent가 project-local
    neutralization transform을 작성한다." 그래서 이 구현은 **읽고 고쳐 쓸 수 있어야** 하며,
    package 내부에서만 가능한 접근을 쓰면 예제가 아니라 거짓말이 된다.

    `extension/templates/`의 transform 템플릿이 이 파일을 출발점으로 삼는다.

업종 분류가 왜 인자인가
    분류는 **재분류될 수 있는 데이터**다(architecture §2.8). 여기서 직접 읽으면 시점이
    적용되지 않아 과거 시점의 중립화가 오늘의 분류로 오염된다.
"""
