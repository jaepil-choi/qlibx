"""주어진 창 안에서만 도는 시계열 연산.

구현할 것
    이동평균 · ewma · 누적합 등. 전부 **입력으로 받은 창을 넘어가지 않는다.**

왜 이것이 핵심인가
    창 밖을 건드릴 수 없음이 **시그니처로 보장된다.** 함수가 받은 사각형 바깥에 접근할 방법이
    없으므로, "미래를 보지 마세요"라는 규칙을 적을 필요가 없다.

    architecture §4.4가 "causal primitive — 창 밖을 건드리지 않음이 구현으로 보장되는 순수
    함수"라고 부른 자리가 여기다.

창이 부족하면
    있는 만큼으로 계산할지 실패할지는 **호출자의 경제적 규칙**이다. 이 함수들이 대신 정하지
    않는다. 창의 requested/actual coverage는 ObservationBatch에 있다.
"""
