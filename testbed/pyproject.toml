[project]
name = "qlibx-conversation-testbed"
version = "0.1.0"
description = "Isolated consumer project for testing qlibx through its public surface"
requires-python = ">=3.12,<3.13"
dependencies = [
    "qlibx",
]

[tool.uv.sources]
qlibx = { path = "../", editable = false }
