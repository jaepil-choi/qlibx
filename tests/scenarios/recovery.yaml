schema_version: 1
suite: qlibx-daily-recovery
support_status: current_readiness_gap
gap_id: GAP-RECOVERY-001
scenario:
  case: process_crash_matrix_real_dw
  test: tests/acceptance/test_recovery_scenarios.py::test_gap_recovery_001_process_crash_matrix_real_dw
  inputs:
    data_kind: real
    sources:
      - data/DW/fng_stock_daily_prices.csv
    fault_kind: process_termination
    crash_points:
      - decision_recovery_published
      - account_fill_committed
      - execution_artifact_published
      - account_mark_committed
      - strategy_memory_committed
      - final_checkpoint_published
  expected:
    uninterrupted_parity: true
    duplicate_decision: false
    duplicate_fill: false
    duplicate_memory_commit: false
    feedback_cursor_parity: true
    artifact_identity_parity: true
identity_guard:
  test: tests/acceptance/test_recovery_scenarios.py::test_gap_recovery_001_changed_identity_requires_explicit_branch
  expected:
    error_code: RESUME_BRANCH_REQUIRED
    mutation_before_failure: false